apiVersion: apps/v1 
kind: Deployment 
metadata: 
  name: web-ui 
spec: 
  replicas: 1 
  selector: 
    matchLabels: 
      name: web-ui-lp 
      component: microservice 
      role: lp 
  template: 
    metadata: 
      labels: 
        name: web-ui-lp 
        component: microservice 
        role: lp 
    spec: 
      containers: 
      - name: web-ui 
        image: gcr.io/lease-prod/web-ui:test2
        imagePullPolicy: Always 
        env: 
          - name: ENV 
            value: dev
          - name: API_ADDRESS 
            value: "minio-api" 
          - name: API_SERVER_PORT 
            value: '8080' 
        ports: 
        - containerPort: 80 
          hostPort: 80 
      nodeSelector: 
        cloud.google.com/gke-nodepool: pool-web 
